language: go

go:
- master

install:
- GO111MODULE=on go get github.com/gohugoio/hugo

script:
- hugo

deploy:
  provider: pages
  skip_cleanup: true
  repo: yeya24/blog
  target-branch: gh-pages
  github_token: $GH_TOKEN
  local_dir: public
  on:
    branch: master
